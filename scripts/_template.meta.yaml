name: <script_name>
type: procedure  # procedure | view | trigger | function
source_tables:
  - SCHEMA.TABLE_A
target_tables:
  - SCHEMA.TABLE_B
calls:
  - other_procedure_name
jobs:
  - JOB_NAME_A
migration_status: pending  # pending | in_progress | done | blocked
notes: |
  Freitext: Besonderheiten, Risiken, offene Fragen
